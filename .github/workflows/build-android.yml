name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: temurin

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Expo Prebuild — generate Android native project
        env:
          EXPO_NO_TELEMETRY: "1"
          CI: "1"
        run: npx expo prebuild --platform android --clean --no-install

      - name: Install dependencies after prebuild
        run: pnpm install --no-frozen-lockfile

      - name: Make Gradle wrapper executable
        working-directory: android
        run: chmod +x gradlew

      - name: Build Release APK
        working-directory: android
        env:
          GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g"
        # Release build embeds the JS bundle into the APK and is signed with
        # the auto-generated debug keystore by default (installable via
        # sideloading). Debug builds expect a live Metro server and hang
        # forever on the splash screen without one — avoid assembleDebug.
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: netflixy-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30
